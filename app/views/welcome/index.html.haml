= #render 'shared/social'
#nc
  %h1 245cloud（ニシコクラウド）で24分間集中しましょう
#fb-root
- if current_user
  :javascript
    window.facebook_id = "#{current_user.facebook_id}";
    window.settings = #{cookies[:settings] || '{}'};
