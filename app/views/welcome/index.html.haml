%h1 245cloudヘビーローテーションランキング
%hr
-@musics_users.each do |mu|
  - music = mu.music
  - user = mu.user
  = link_to image_tag(music.icon2), "/musics/#{music.id}"
  ='=>'
  = link_to image_tag(user.icon), "/#{user.id}"
  %hr

%hr
